# 📝 Изменения - Добавлены уведомления о долгах

## Что было добавлено

### 1. Новый модуль `client-notifications.js`
- **Функция `findClientsWithPurchaseOnDate()`** - поиск клиентов, которые покупали в определенную дату
- **Функция `findDebtorsWithPurchaseOnDate()`** - поиск должников среди клиентов, покупавших в определенную дату
- **Функция `calculateDebts()`** - расчет долгов клиентов
- **Функция `formatNumber()`** - форматирование чисел

### 2. Изменения в `bot.js`

#### Добавлен импорт модуля:
```javascript
// 📢 Загружаем модуль уведомлений о клиентах
const clientNotifications = require('./client-notifications');
```

#### Обновлена клавиатура отчетов:
```javascript
const reportsKeyboard = Markup.keyboard([
    ['📈 Приход за период', '📉 Расход за период'],
    ['💵 Погашения за период', '👥 Топ должников'],
    ['🚂 Итоги вагонов', '👤 Карточка клиента'],
    ['🔔 Уведомления о долгах', '🔙 Назад']  // ← НОВАЯ КНОПКА
]).resize();
```

#### Добавлены новые обработчики:

1. **Главный обработчик уведомлений:**
   ```javascript
   bot.hears(/🔔|уведомления о долгах/i, async (ctx) => {
       // Показывает кнопки выбора периода (7/14/30 дней)
   });
   ```

2. **Обработчик выбора периода:**
   ```javascript
   bot.action(/^notify_(\d+)$/, async (ctx) => {
       // Ищет должников и формирует отчет
   });
   ```

3. **Обработчик экспорта в Excel:**
   ```javascript
   bot.action(/^exnotify_(\d+)$/, async (ctx) => {
       // Создает Excel файл с уведомлениями
   });
   ```

### 3. Тестовые файлы
- **`test-notifications.js`** - тесты функциональности
- **`УВЕДОМЛЕНИЯ-О-ДОЛГАХ.md`** - подробная инструкция
- **`БЫСТРЫЙ-СТАРТ-УВЕДОМЛЕНИЯ.md`** - краткая инструкция

## Как работает

1. **Пользователь выбирает период** (7, 14 или 30 дней назад)
2. **Система ищет клиентов**, которые покупали в эту дату
3. **Проверяет долги** каждого найденного клиента
4. **Показывает результат** с детальной информацией
5. **Предлагает экспорт** в Excel для дальнейшей работы

## Логика работы

```
Дата покупки = Сегодня - N дней
↓
Поиск всех покупок в эту дату
↓
Фильтрация клиентов с долгами
↓
Группировка по клиентам
↓
Расчет общих долгов и сумм покупок
↓
Формирование отчета
```

## Пример использования

**Задача:** Напомнить клиентам о долгах через неделю после покупки

**Решение:**
1. Каждую неделю заходить в "🔔 Уведомления о долгах"
2. Выбирать "📅 7 дней назад"
3. Получать список должников, которые покупали неделю назад
4. Связываться с ними для напоминания о долге

## Преимущества

✅ **Точность** - показывает клиентов, которые покупали именно в нужную дату  
✅ **Полнота** - вся информация о долгах и покупках в одном месте  
✅ **Удобство** - простой интерфейс с кнопками  
✅ **Экспорт** - возможность сохранить данные в Excel  
✅ **Гибкость** - выбор периода (7/14/30 дней)  

## Технические детали

- **Фильтрация удаленных записей** - учитывает только активные данные
- **Группировка по клиентам** - если клиент покупал несколько раз в день
- **Сортировка по долгу** - большие долги показываются первыми
- **Безопасность** - проверка авторизации пользователя
- **Производительность** - эффективные алгоритмы поиска

## Совместимость

- ✅ Работает с существующей структурой данных
- ✅ Не влияет на другие функции бота
- ✅ Использует те же права доступа
- ✅ Поддерживает фильтрацию по группам складов

---

**Дата внедрения:** 2 февраля 2026  
**Разработчик:** AI Assistant  
**Статус:** ✅ Готово к продакшену  
**Тестирование:** ✅ Пройдено