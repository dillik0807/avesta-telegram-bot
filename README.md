# 🤖 Telegram бот для системы Avesta

Бот для просмотра остатков складов и долгов клиентов из системы учёта товаров.

## 📋 Возможности

- 📦 Просмотр остатков по всем складам
- 💰 Просмотр долгов клиентов
- 📊 Краткая сводка по системе
- 🔄 Данные в реальном времени из Firebase

## 🚀 Установка

### 1. Создайте бота в Telegram

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Введите имя бота (например: `Avesta Склад`)
4. Введите username бота (например: `avesta_warehouse_bot`)
5. Скопируйте полученный токен

### 2. Настройте конфигурацию

```bash
# Перейдите в папку бота
cd telegram-bot

# Скопируйте пример конфигурации
copy .env.example .env

# Отредактируйте .env файл
notepad .env
```

Заполните `.env` файл:

```env
# Токен от @BotFather
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz

# URL вашей Firebase базы данных
FIREBASE_DATABASE_URL=https://warehouse-system-f4e52-default-rtdb.asia-southeast1.firebasedatabase.app

# ID пользователей Telegram, которым разрешён доступ
# Узнать свой ID можно у @userinfobot
ALLOWED_USERS=123456789,987654321

# Рабочий год
CURRENT_YEAR=2025
```

### 3. Установите зависимости

```bash
npm install
```

### 4. Запустите бота

```bash
npm start
```

## 📱 Использование

После запуска бота, откройте его в Telegram и отправьте `/start`.

### Команды:

| Команда | Описание |
|---------|----------|
| `/start` | Главное меню |
| `/stock` | Остатки складов |
| `/debts` | Долги клиентов |
| `/summary` | Краткая сводка |
| `/help` | Справка |

### Кнопки меню:

- 📦 **Остатки складов** - показывает текущие остатки по всем складам
- 💰 **Долги клиентов** - список должников с суммами
- 📊 **Краткая сводка** - общая статистика системы
- 🔄 **Обновить данные** - информация об обновлении

## 🔒 Безопасность

- Доступ к боту ограничен списком `ALLOWED_USERS`
- Если список пуст - доступ открыт всем
- Узнать свой Telegram ID можно у бота [@userinfobot](https://t.me/userinfobot)

## 🖥️ Запуск как служба (Windows)

Для постоянной работы бота можно использовать PM2:

```bash
# Установка PM2
npm install -g pm2

# Запуск бота
pm2 start bot.js --name avesta-bot

# Автозапуск при перезагрузке
pm2 startup
pm2 save
```

## 🐧 Запуск на Linux/VPS

```bash
# Установка Node.js (если не установлен)
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs

# Клонирование и настройка
cd /opt
git clone <your-repo> avesta-bot
cd avesta-bot/telegram-bot
npm install
cp .env.example .env
nano .env  # Заполните конфигурацию

# Запуск через PM2
pm2 start bot.js --name avesta-bot
pm2 startup
pm2 save
```

## 📝 Логи

```bash
# Просмотр логов PM2
pm2 logs avesta-bot

# Просмотр статуса
pm2 status
```

## ❓ Решение проблем

### Бот не отвечает
- Проверьте правильность токена в `.env`
- Убедитесь что бот запущен (`pm2 status`)
- Проверьте логи (`pm2 logs avesta-bot`)

### Ошибка доступа к Firebase
- Проверьте URL базы данных
- Убедитесь что правила Firebase разрешают чтение

### Нет доступа к боту
- Добавьте свой Telegram ID в `ALLOWED_USERS`
- Узнайте ID у [@userinfobot](https://t.me/userinfobot)
