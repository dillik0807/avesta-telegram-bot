# 🚀 Деплой бота на Railway

## Шаг 1: Регистрация на Railway

1. Перейдите на https://railway.app
2. Нажмите "Login" → войдите через GitHub (рекомендуется)
3. Подтвердите email если нужно

## Шаг 2: Создание репозитория на GitHub

1. Перейдите на https://github.com/new
2. Название: `avesta-telegram-bot`
3. Тип: Private (приватный)
4. Нажмите "Create repository"

## Шаг 3: Загрузка кода на GitHub

Откройте терминал в папке `telegram-bot` и выполните:

```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/ВАШ_ЛОГИН/avesta-telegram-bot.git
git push -u origin main
```

## Шаг 4: Деплой на Railway

1. На Railway нажмите "New Project"
2. Выберите "Deploy from GitHub repo"
3. Найдите и выберите `avesta-telegram-bot`
4. Railway автоматически начнёт деплой

## Шаг 5: Настройка переменных окружения

В Railway откройте ваш проект → вкладка "Variables" → добавьте:

### TELEGRAM_BOT_TOKEN
```
8552657743:AAFJH8A1xq8jq9ECgCcB_5qJdHY2QK4MaTM
```

### FIREBASE_DATABASE_URL
```
https://warehouse-system-f4e52-default-rtdb.asia-southeast1.firebasedatabase.app
```

### FIREBASE_SERVICE_ACCOUNT
Скопируйте ВЕСЬ контент файла `firebase-service-account.json` и вставьте как значение.
Это должен быть JSON в одну строку, например:
```
{"type":"service_account","project_id":"warehouse-system-f4e52",...}
```

**Важно:** Откройте файл `firebase-service-account.json`, скопируйте всё содержимое и вставьте в это поле.

## Шаг 6: Перезапуск

После добавления переменных Railway автоматически перезапустит бота.

## Проверка

1. Откройте вкладку "Deployments" в Railway
2. Нажмите на последний деплой
3. Посмотрите логи - должно быть:
   ```
   ✅ Firebase Admin SDK инициализирован (из ENV)
   🤖 Бот запускается...
   ✅ Бот подключен: @asevata2026_bot
   ```

## Готово! 🎉

Теперь бот работает 24/7 на сервере Railway!

---

## Полезные команды

### Посмотреть логи
В Railway: Deployments → выберите деплой → Logs

### Перезапустить бота
В Railway: Settings → Restart

### Остановить бота
В Railway: Settings → Remove Service

---

## Лимиты бесплатного плана Railway

- $5 бесплатных кредитов в месяц
- Этого хватает на ~500 часов работы бота
- Бот может работать весь месяц если не превышать лимиты

Если кредиты закончатся, можно:
1. Добавить карту и платить ~$5/месяц
2. Перейти на другой бесплатный сервис (Render, Fly.io)
