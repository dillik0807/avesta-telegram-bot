# Настройка Telegram бота Avesta

## Проблема с доступом к Firebase

Бот не может получить данные из Firebase потому что правила базы данных требуют авторизацию.

## Решение 1: Изменить правила Firebase (простой способ)

1. Откройте Firebase Console: https://console.firebase.google.com
2. Выберите проект `warehouse-system-f4e52`
3. Перейдите в Realtime Database → Rules
4. Измените правила на:

```json
{
  "rules": {
    ".read": true,
    ".write": "auth != null"
  }
}
```

⚠️ Это разрешит чтение всем, но запись только авторизованным.

## Решение 2: Использовать Firebase Admin SDK (безопасный способ)

1. В Firebase Console перейдите в Project Settings → Service Accounts
2. Нажмите "Generate new private key"
3. Сохраните файл как `telegram-bot/firebase-service-account.json`
4. Установите пакет: `npm install firebase-admin`
5. Бот автоматически использует Admin SDK если найдёт файл

## Использование бота

После настройки Firebase:

1. Запустите бота: `node bot.js`
2. Найдите @asevata2026_bot в Telegram
3. Отправьте /start
4. Нажмите "🔐 Войти"
5. Введите логин и пароль из приложения Avesta

## Команды бота

- `/start` - Главное меню
- `/stock` или 📦 - Остатки на складах
- `/debts` или 💰 - Долги клиентов
- `/summary` или 📊 - Общая сводка
- `/logout` или 🚪 - Выйти
